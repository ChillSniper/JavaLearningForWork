#

## **一、项目和经历相关**

1. 你先做一下自我介绍。
2. 你说的高并发项目？有做过压测吗？怎么压的，指标怎么样？
3. 这个岗位是 Python 后端，你虽然项目主要是 Java，那你的 Python 掌握程度怎么样？
4. 你项目里 Redis 是拿来做什么的？
5. 为什么这里要用 Redis，不直接用本地内存里的字典 / 映射？
6. JWT 的原理是什么？
7. 既然已经有 HTTPS，为什么你这里还要自己做加密？
8. 你这里有用到依赖注入吗？
9. 云端多端同步场景下，JWT 是怎么续期和下发的？是单实例续期，还是每个实例都能续期和下发？
10. 你做这些项目时遇到过什么性能瓶颈？你是怎么分析、怎么优化的？

## **二、Python / 后端基础**

1. 你看过 Python 相关的书吗，比如源码剖析、Effective Python 之类的？
2. Python 全局锁 GIL 是什么？
3. Python 协程用过吗？
4. 你说说协程和线程、进程的区别。
5. 死锁了解吗？
6. 你遇到过死锁吗？
7. 怎么避免死锁？除了固定加锁顺序，还有什么办法？
8. HTTP 状态码里，401 是什么？
9. SQL 注入攻击了解吗？
10. RPC 是什么？

## **三、数据库相关**

1. 数据库有没有深入学过？
2. MySQL 的性能优化手段有哪些？
3. 为什么一般主键更常用自增 ID，而不是 UUID？
4. 更新一条数据时，可能会用到什么锁？

## **四、Redis 和中间件相关**

1. Redis 底层原理了解吗？比如它为什么快？
2. Redis 的有序集合 zset 底层是基于什么数据结构实现的？
3. Redis 的持久化方式有哪些？
4. AOF 会一直增长，那怎么处理？
5. Docker 了解吗，用过吗？
6. K8s 了解吗？
7. LRU 是什么策略？
8. LRU 底层是怎么实现的？

## **五、场景设计题**

1. 给你一个包含十几万个视频的目录，要在网页里展示视频缩略图；但生成缩略图会调用 ffmpeg，比较耗时、可能阻塞，而且 QPS 还高。你怎么设计这个接口？
